/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    distDir: 'dist',
    // basePath: '/test-task-user-record',
    // assetPrefix: '/test-task-user-record/',
};

export default nextConfig;
